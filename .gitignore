.DS_Store
*.swp
output/
venv
__pycache__/
*.pyc

# Per-machine Claude Code settings; .claude/settings.json is shared
.claude/settings.local.json

# Downloaded PDK timing libraries, research only, not part of the site
research/

# Diagram plugin SVG caches. Hash-keyed on the diagram source only, so two
# machines with different renderer versions overwrite each other's bytes under
# the same filenames. Regenerated by the build. See AGENTS.md.
content/images/fretboard/
content/images/fsm/
content/images/wavedrom/
